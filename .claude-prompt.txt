你是 Interview-Practice-App 的全栈开发工程师。MVP 代码已完成，但从未做过端到端联调验证。

## 目标
启动后端 + 前端，注入测试数据，走通完整流程，修复发现的所有 bug。

## 项目路径
D:\AI_Project\Surprise\Interview-Practice-App

## 技术栈
- 后端：FastAPI + SQLModel + PostgreSQL（Docker）+ Alembic
- 前端：Next.js 14 + TypeScript + Tailwind CSS + axios
- AI：DashScope (LLM Gateway)
- Python 环境：conda env "pytorch_gpu" (D:\AI_Envs\pytorch_gpu)

## 当前状态
- PostgreSQL 容器 docker-db_postgres-1 正在运行（端口 5432）
- 后端 .env 配置已就绪，数据库 URL: postgresql+asyncpg://postgres:dev_password@localhost:5432/interview_practice
- 前端编译通过 (npm run build OK)
- git 最新 commit 是 9965c64（修复了 MissingGreenlet 错误）

## 具体任务（按顺序执行）

### 任务1：启动后端
1. 进入 backend/ 目录，conda activate pytorch_gpu 环境
2. 确认数据库表已迁移：运行 `alembic upgrade head`
3. 启动 FastAPI：`uvicorn app.main:app --reload --port 8000`
4. 验证后端：curl http://localhost:8000/api/v1/questions/
5. 如果启动失败，修复问题

### 任务2：注入测试数据（通过 API）
通过后端 API 创建 15-20 条测试题目，覆盖这些领域：
- RAG（3-5题）：如 "什么是 RAG？"、"RAG 中 retrieval 和 generation 如何协同？"
- Backend（3-5题）：如 "RESTful API 设计原则"、"什么是幂等性？"
- Database（2-3题）：如 "什么是 B+ 树？"、"事务的 ACID 特性"
- Frontend（2-3题）：如 "React 虚拟 DOM 原理"、"什么是 SSR？"
- ML（2题）：如 "什么是过拟合？如何防止？"

同时创建几条 study_records 用于统计页面展示。
写一个 Python 脚本（放在 backend/ 目录下）来批量插入测试数据，然后运行它。脚本用 conda activate pytorch_gpu 环境执行。

### 任务3：前端联调
1. 启动前端：在 web/ 目录下 `npm run dev`（端口 3000）
2. 逐一访问各页面确认正常：
   - / 首页（统计卡片 + 最近题目）
   - /questions（列表 + 搜索筛选）
   - /questions/[id]（详情）
   - /import（导入页）
   - /study（学习记录）
   - /interview（模拟面试）
   - /stats（统计面板）
3. 发现任何报错/白屏/数据不显示，修复它

### 任务4：收尾
- git commit 所有修复（每个修复一个 commit）
- npm run build 确认前端编译通过

## 约束
- 后端用 conda activate pytorch_gpu 环境
- 不修改 .cursorrules 或 IDE 配置
- 需要用户决策的问题先记录下来
- 每次修复后 git commit

完成后请告诉我最终状态，包括修复了多少个 bug、注入了多少条数据。
