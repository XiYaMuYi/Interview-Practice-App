# Task Batch 3: 全局搜索 + 最终优化

## 项目根目录
`D:\AI_Project\Surprise\Interview-Practice-App`

## Python 环境
`D:\AI_Envs\pytorch_gpu\python.exe`

## 背景
MVP 核心功能已完成：
- ✅ 文本/文件导入 + 题目提取
- ✅ 题目列表 + 筛选
- ✅ 题目详情 + AI 讲解
- ✅ 学习练习（答题 → 评分 → 反馈）
- ✅ SM-2 复习调度
- ✅ 学习统计

需要补充：
- **全局搜索** — 用户输入关键词，搜索题目内容/标题
- **最终优化** — 验证所有功能端到端可用

## 任务

### 1. 后端：添加全局搜索 API

在 `backend/app/api/v1/routes/question_routes.py` 中添加搜索端点：

```python
@router.get("/search")
async def search_questions(
    session: DbSession,
    q: str = Query(..., min_length=1),
    limit: int = Query(20, ge=1, le=100),
):
    """Search questions by title/content keywords."""
```

实现方式：
- 使用 SQL LIKE 查询 `title` 和 `content` 字段
- 如果 pgvector 已配置，优先使用向量相似度搜索
- 返回匹配的题目列表（与现有 QuestionListItem 格式一致）

### 2. 前端：添加搜索功能

**选项 A**：在题目列表页 `web/src/app/questions/page.tsx` 顶部添加搜索框
- 用户输入关键词后调用 `/api/v1/questions/search?q=...`
- 搜索结果替换原有列表展示
- 搜索框支持回车搜索

**选项 B**：在全局导航栏添加搜索图标
- 点击弹出搜索框
- 搜索结果页面或下拉展示

选择选项 A（更简单直接）。

### 3. 最终验证
- 确保所有页面 TypeScript 编译通过
- 确保 `npm run build` 成功
- 检查所有 API 路由是否正确注册

## 验收标准
1. 搜索 API 返回 200 且有正确结果
2. 前端可以搜索并看到结果
3. `npm run build` 成功
4. 所有现有功能不受影响
